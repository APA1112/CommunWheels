{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="table_container">
            <div class="button_container">
                <a href="#" class="primaryButton"><i class="ti ti-table-plus"></i>Generar Cuadrante</a>
            </div>
            <table class="tabla">
                <thead>
                <tr>
                    <th>Grupo</th>
                    <th>Fecha</th>
                </tr>
                </thead>
                <tbody>
                {% for timeTable in timeTablesGroup %}
                    <tr>
                        <td><a href="{{ path('app_trip', {'id':timeTable.id}) }}">{{ timeTable.band.name }}</a></td>
                        <td>{{ timeTable.weekStartDate|date('d-m-Y') }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2">No se encontraron registros</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            <div class="button_container">
                <a href="{{ path('timetable_main') }}" class="primaryButton"><i class="ti ti-table-plus"></i>Volver</a>
            </div>
        </div>
    </div>
{% endblock %}

