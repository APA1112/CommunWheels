{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="button_container">
            <a href="#" class="primaryButton"><i class="ti ti-table-plus"></i>Generar Cuadrante</a>
            <a href="{{ path('app_non_school_day') }}" class="primaryButton"><i class="ti ti-table-plus"></i>Dias festivos</a>
        </div>
        <div class="table_container">

        </div>
    </div>
{% endblock %}