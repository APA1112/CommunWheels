{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="button_container">
            <a href="{{ path('driver_new') }}" class="primaryButton"><i class="ti ti-table-plus"></i>Nuevo conductor</a>
        </div>
        <div class="table_container">
            <table class="tabla">
                <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Email</th>
                    <th>Asientos</th>
                    <th>Dias conducidos</th>
                    <th>Tiempo de espera</th>
                </tr>
                </thead>
                <tbody>
                {% for driver in pagination %}
                    <tr>
                        <td>
                            <a href="{{ path('driver_mod', {'id':driver.id}) }}">
                                {{ driver.name}} {{ driver.lastName }}
                            </a>
                        </td>
                        <td>{{ driver.email }}</td>
                        <td>{{ driver.seats }}</td>
                        <td>{{ driver.daysDriven }}</td>
                        <td>{{ driver.waitTime }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            {# display navigation #}
            <div class="paginator">
                {{ knp_pagination_render(pagination) }}
            </div>
        </div>
    </div>
{% endblock %}